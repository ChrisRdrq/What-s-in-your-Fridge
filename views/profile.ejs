<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>What's in My Fridge?</title>
  </head>
  <body>
    <h1>New Ingredients</h1>
    <form method="POST" action="ingredients/add">
      <label>Ingredients: <input type="text" name="ingredients" id="ingredients" placeholder="Add Food Here"></label>
      <br>
      <input type="submit" value="Submit">
    </form>

    <ul>
      <% if (user.ingredients.length > 0) { %>
          <% for (let i=0; i < user.ingredients.length; i++) { %>
              <li> <%= user.ingredients[i] %> </li>
          <% } %>
      <% } %>
    </ul>

  </body>
</html>
